# Infragistics Grid POC (Angular 20 + Angular Elements)

Run `npm install`, then `npm run build`, then `npm run serve:dist` to view demo at http://localhost:4209/demo/
# infragistics-grid-poc
# infragistics-poc
